import React from 'react';
import Spinner from './Spinner';
const Cargando = () => {
    return (
        <div style={{paddingTop: '25rem'}}>
            <Spinner></Spinner>
        </div>
    );
}
 
export default Cargando;